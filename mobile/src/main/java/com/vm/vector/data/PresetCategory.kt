package com.vm.vector.data

/**
 * Preset categories for workout, routine, and diet weekly plans.
 */
enum class PresetCategory(val fileSuffix: String) {
    Workout("workout"),
    Routine("routine"),
    Diet("diet")
}
